# Python
.venv/
__pycache__/
*.pyc

# Node / Vite
overlay_app/node_modules/
overlay_app/build/

# Retratos descargados con backend/scripts/download_portraits.py -
# se regeneran, no tiene sentido versionar binarios pesados en git.
overlay_app/public/portraits/
overlay_app/public/portraits-large/

# Logos subidos desde la pantalla de Transmisión (BroadcastSettingsScreen) -
# mismo criterio que los retratos, son binarios subidos por el staff.
overlay_app/public/branding/

# PyInstaller
build/
dist/
*.spec
# ...excepto el spec real del proyecto (Fase 5, ver ROADMAP.md) - no es
# un auto-generado descartable, tiene datas/hiddenimports a mano que no
# se recrean solos con un "pyinstaller main.py" nuevo.
!tdf_random_select.spec

# Datos locales del draft (SQLite) y config sensible
*.db
.env

# Editor
.vscode/
.idea/
